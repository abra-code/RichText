<?xml version="1.0" encoding="utf-8"?>
<!-- RichText renders remote images through AsyncImageCache, which contributes the INTERNET permission via
     manifest merging. The library itself declares no components and no additional permissions. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
</manifest>
