A `code span` in a sentence.

Code with `double backtick ``inside`` ` delimiters.

Code preserves *asterisks* and **markup** literally: `not **bold**`.

Code preserves `<html>` and `&amp;` characters.

Unterminated `code span renders literally.

Empty `` backticks.

Adjacent `one``two` spans.
